# Enumeration

```
nmap -vv -sV --version-intensity=5 -sU -Pn -p 161,162 --script=snmp-netstat,snmp-processes $IP
```

# OneSixtyOne

```
onesixtyone -c /opt/useful/SecLists/Discovery/SNMP/snmp.txt -i $IP
```


# SnmpWalk

```
Only once we have the community string, we can try to enumerate the system information and retrieve the `OIDs` only with SNMP service that does not require authentication (version 1, 2c) :

# Query the OIDs w/ their info
snmpwalk -v2c -c public $IP

Once we know the `community string` , we can try to bruteforce the `OIDs` and enumerate the information behind them.

# Default syntax example
braa <community string>@<IP>:.1.3.6.*

# Bruteforce OIDs of a community string
braa public@10.129.14.128:.1.3.6.*
```


# Dangerous Settings

```
# Conf file
/etc/snmp/snmp.conf
```

Some dangerous settings that the administrator can make with SNMP are :

|Dangerous settings|**Description**|
|---|---|
|`rwuser noauth`|Provides access to the full OID tree without authentication.|
|`rwcommunity <community string> <IPv4 address>`|Provides access to the full OID tree regardless of where the requests were sent from.|
|`rwcommunity6 <community string> <IPv6 address>`|Same access as with `rwcommunity` with the difference of using IPv6.|